#include <iostream>
#include <string>
//#include "task3"

using namespace std;

/*int main()
{
     AdminManager::printClientMenu();
    Client* client = AdminManager::login(12345, "password");
    if (client != nullptr) {
        AdminManager::AdminOptions(client);
        delete client;
    } else {
        cout << "Login failed. Invalid ID or password." << endl;
    }

*/
